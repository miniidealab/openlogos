# Demo SaaS project (placeholder)

This folder is a **minimal stub** (AGENTS + `logos/` config) for a fictional SaaS stack.

For a **complete, runnable OpenLogos reference** (desktop app + full `logos/resources/` + `openlogos verify`), use:

**[../flowtask/](../flowtask/)** (Tauri; not an HTTP API service)
