---
description: "OpenLogos — create change proposal / 创建变更提案（用法见正文）"
---

用法：`/openlogos-change <slug>`（例如 `/openlogos-change add-login`）。slug 仅允许小写字母、数字与连字符。

!`openlogos change $ARGUMENTS`

若参数为空，请提示用户补充 slug。
