---
description: "OpenLogos — generate AI-ready resource_index update prompt / 生成 AI 驱动的索引更新 Prompt"
---

在项目根执行：

!`openlogos index`

将终端输出告知用户；若成功，提示用户：「请读取 `logos/index-prompt.md` 并执行其中的更新指令」，AI 将读取各文件内容后直接更新 `logos/logos-project.yaml` 的 `resource_index` 字段。
