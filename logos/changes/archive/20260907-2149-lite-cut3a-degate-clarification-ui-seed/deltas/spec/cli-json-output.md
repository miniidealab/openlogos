# Delta: cli-json-output.md

> change: lite-cut3a-degate-clarification-ui-seed
> 目标：`spec/cli-json-output.md`

## REMOVED — Plan 阶段决策澄清 JSON 契约（clarification@1）

本节定义 `status` / `next` 的 `plan_state.clarification` 投影：`schema` / `mode` / `status` / `required_categories` / `unresolved` 字段结构与 pending/complete 语义。决策澄清自本提案起是纯文档、不再被解析（§2.74.1），该投影不再发射，本节失去定义对象。

envelope 的通用结构与其余 `plan_state` 字段不受影响。
