{"ts":"2026-09-08T02:24:46.861Z","trigger":"merge","old_code":"## [code] 代码实现\n\n> 六维打分：影响范围 2（删 5 个源文件 + 重写 merge.ts + 新增 lint-specs.ts + 收敛 index/next/status 接线 + 5 个测试文件整删）、行为复杂度 2（merge 直接合并须复现 test_change_set 结构化字段并保证失败整批回滚；lint-specs 三类结构检查）、契约变化 2（删事务 envelope + 新增 merge/lint-specs 输出合同 + SPEC_MERGED 结构变更）、测试规模 1（3 个新 ID + 107 个删除）、风险 2（merge 是全流程枢纽，改错即无法合并任何提案）、不确定性 0（1a 已验证同构方法；合并引擎与落盘原语原地复用）→ 9 分，大任务，垂直拆为 2 片。\n> 删后续证伪门：切片1 删后续（切片2）后独立过全量 verify——merge 直接合并自带 UT-S09-340/341，不依赖 lint-specs；(b) 端到端可观察：一次调用完成合并、失败整批回滚。✓\n> 切片2 为末片无后续可删；(b) 端到端可观察：`openlogos lint-specs` 报出重复 ID 且不阻断任何门。✓\n> 为何不合并为单片：切片2 是切片1 删除 seal preflight 后的能力补偿，二者可独立验收（前者验合并正确性，后者验结构检查可用且不成门）；分片让「删除」与「补偿」各自留下可追溯的验收证据。为何不倒序：lint-specs 先落地则其补偿对象尚未删除，(b) 端到端价值不成立。\n- [ ] 切片1：merge 改直接合并 + 删合并事务外壳——重写 `cli/src/commands/merge.ts`：解析 [delta] 目标集做 P==T==D 与路径合法性校验（前置于写入）→ 逐 canonical target 调 `composeOpenLogosMarkdown` 合成最终字节（内含锚唯一定位、标题 rebase、`verifyAgentMaterialOutcome` 物质结果复验）→ 交 `applyBaselineClosureBatch` 一次性原子落盘（失败整批回滚）→ 末步写含结构化 `test_change_set` 的 `SPEC_MERGED`（`type: merge_complete` + `completed_at` + `test_change_set`，去掉 transaction_id/seal_sha256/receipt_sha256 三个事务字段）；稳定错误码 MERGE_NO_ACTIVE_CHANGE / MERGE_DELTA_INVALID / MERGE_TARGET_MISMATCH / MERGE_ALREADY_COMPLETE / MERGE_APPLY_FAILED，失败零副作用且 message 附 `git checkout logos/resources/` 回滚提示；删 `lib/merge-transaction.ts`(1214)、`lib/merge-transaction-candidate.ts`(128)、`lib/merge-transaction-semantic.ts`(170)、`commands/merge-transaction.ts`(164)、`commands/merge-apply.ts`(364)，并从 `src/index.ts` 摘除 merge transaction / merge-apply 路由、从 `commands/next.ts` 与 `commands/status.ts` 摘除 merge_transaction 投影；`lib/markdown-section-authority.ts`、`lib/test-change-set.ts`、`lib/baseline-apply.ts` 逐行保留；整删 5 个事务专属测试文件（merge-transaction、merge-frontier-flow、merge-reopen-forward、s09-merge-terminal-outcome、nested-section-anchor-smoke，共 2,101 行 / 107 个 ID）；同步实现含 OpenLogos reporter 的 UT-S09-340、UT-S09-341\n- [ ] 切片2：lint-specs 独立规格结构检查——新增 `cli/src/commands/lint-specs.ts` 实现 `openlogos lint-specs [--format json]`：扫描 `logos/resources/test/` 全部测试规格，检查重复 ID（同一 UT/ST/SMOKE 在表格首列出现多次）、表格列数一致性、ID 格式合法性；输出 envelope 含 checked_files 与 issues（code/path/line/message），发现问题非零退出；接入 `src/index.ts` 路由；**不参与任何门**——merge/verify/archive/change-lint 均不读取其结论、不因其结果阻断，以此补偿 seal preflight 删除后的结构检查能力空缺；同步实现含 OpenLogos reporter 的 ST-S09-140（端到端：合并 → git 回滚重来无需 reopen → lint-specs 报出重复 ID → 同一状态下 merge 与 verify 均不被其阻断）"}
{"ts":"2026-09-08T02:39:54.926Z","trigger":"merge","old_code":"## [code] 代码实现\n\n- [x] 切片1：merge 改直接合并 + 删合并事务外壳——重写 `cli/src/commands/merge.ts` 走新的 `lib/merge-direct.ts`：解析 [delta] 目标集做 P==T==D 与路径合法性校验（前置于写入）→ 逐 canonical target 调 `composeOpenLogosMarkdown` 合成最终字节（内含锚唯一定位、标题 rebase、`verifyAgentMaterialOutcome` 物质结果复验）→ 交 `applyBaselineClosureBatch` 一次性原子落盘（失败整批回滚）→ 末步写含结构化 `test_change_set` 的 `SPEC_MERGED`；删除 `lib/merge-transaction{,-candidate,-semantic}.ts`、`commands/merge-transaction.ts`、`commands/merge-apply.ts` 与 index/next/status 的接线及 4 个事务测试文件；二次合并后切片归属消费（S32）随之改写。\n- [x] 切片2：lint-specs 独立规格结构检查——新增 `cli/src/commands/lint-specs.ts`，对 `logos/resources/test/` 做重复 ID / 表格列数 / ID 格式三项只读检查（ID 语法一律复用 `lib/test-id.ts` 唯一权威，ID 表按结构识别而非表头措辞），接入 index 路由与 help；**不参与 merge / verify / archive / change-lint 任何门**，仅非零退出并逐条列出位置供人判断。"}
