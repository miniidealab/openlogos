{"ts":"2026-08-01T16:25:08.234Z","trigger":"merge","old_code":"## [code] 代码实现\n\n> 测试 ID 显式复用声明（门以 proposal.md `## 复用测试 ID` 小节为准，此处同步保留）：复用以下已合并的既有归档链路回归锚点作为证伪面 —— ST-S09-01（S09 提案生命周期）、ST-S11-08（归档前置门禁 verify PASS 后才可归档）、UT-S24-AE-01（全自动归档信号 auto_execute + command=archive）、ST-S24-AE-01（全自动端到端归档信号契约）。\n\n> 删后续自检（slice-planner）：六维打分 11（影响范围 2：新增 `archive-watch.ts` + 改 `archive.ts`/`i18n` + 根 spec 两文件及 dogfood 副本 + 测试；行为复杂度 2：握手状态机 / 多实例 ACK 屏障 / 去递归 / 三态调和 / 能力分层；契约变化 1：新增稳定错误码 `ARCHIVE_WATCH_*`，不上 stdout envelope；测试规模 2；风险 2：Windows 文件语义、本机不可端到端验证；不确定性 2：协议对端在 RunLogos、专属规格未合入）≥ 8，属大任务。\n> 垂直拆分证伪：`win32` 门控使整个握手分支在 mac 上完全不执行，mac 全量 verify 只覆盖非 Windows 路径（既有归档回归锚点）——对任何 Windows 子能力（路径解析 / 租约发现 / prepare-ACK / 去递归 token / 三态调和 / 能力分层）都无法在本环境做「端到端可观察」证伪，删后续门在 mac 退化失效；且各子能力共享 `archive.ts` 同一 win32 分支入口、互相咬合，无法拆成各自在 Windows 上闭环的片。按逃生口（SKILL Step 5）单切——大任务但不可安全垂直拆分。\n> 覆盖边界（如实标注）：本提案专属规格 §2.31 与专属测试 UT-S09-125~134 因 delta 丢失**未合入主规格**，故切片标注的「已合并」锚点仅为既有归档回归锚点，只证伪「握手改造是否破坏既有归档语义」。Windows 握手协议本身的正确性由 code 阶段随源新增的注入式（fs/platform/env 注入）单测（纯逻辑可在 mac 跑）+ 用户在 Windows 机器的端到端验证共同保证，**不在 mac verify 覆盖内——mac verify 绿不代表 Windows 握手已验证**（见项目 Windows 跨机验证约定）。\n\n- [ ] 单切片：Windows 外部归档 watcher 握手 CLI 消费端整体落地 —— 新增 `cli/src/lib/archive-watch.ts`（协议路径解析 + 兼容测试向量 / 实例租约快照过滤 / prepare 原子写 / ACK 轮询屏障 / result 写入 / 去递归 token 严格校验 / 过期清理 / live-archive-guard 三态调和 / 稳定错误码 `ARCHIVE_WATCH_PREPARE_FAILED`·`ACK_TIMEOUT`·`INSTANCE_FAILED`·`STATE_INCONSISTENT`）；`cli/src/commands/archive.ts` 增加 `process.platform === 'win32'` 门控与握手编排（快照空走快路径、非 Windows 路径逐字节不变、fail-closed 不 rename 不动 guard）；`cli/src/i18n.ts` 补 `archive.watch.*` 文案；注入式 fs/platform/env 单测覆盖协议纯逻辑，写入 `cli/test/s09-change.test.ts` 并含 OpenLogos reporter；同步根 `spec/cli-json-output.md` §6.1 错误码表新增 `ARCHIVE_WATCH_*` 与 `spec/workflow.md` archive 段 Windows 握手说明及 `logos/spec/` dogfood 副本；既有归档回归锚点必须持续通过（覆盖 ST-S09-01、ST-S11-08、UT-S24-AE-01、ST-S24-AE-01）"}
