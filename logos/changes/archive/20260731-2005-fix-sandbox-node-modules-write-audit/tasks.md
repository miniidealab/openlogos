# 实现任务

## [delta] 规格变更
- [x] 产出 delta 文件到 `deltas/prd/1-product-requirements/core-01-requirements.md` — MODIFIED S13 / S19 的「异常：sandbox always 无法隔离」验收条件：非白名单写入判定补充依赖目录豁免限定（完整路径段严格等于 `node_modules`），WHEN 增补 symlink 逃逸情形
- [x] 产出 delta 文件到 `deltas/prd/2-product-design/1-feature-specs/core-01-feature-specs.md` — MODIFIED 沙箱执行器规格：新增「依赖目录豁免」不变量（完整路径段严格等于 `node_modules` 才豁免、快照遍历跳过、`sandbox.infos` 信息级通道不改变 status）、symlink 隔离与运行期写保护不变量（保持链接字面量 + realpath containment + OS 级运行期写保护阻断动态逃逸 + 能力分层 always FAIL / auto warn）、白名单定点采集回收（白名单优先于豁免）；同步 §S13 / §S19 验收摘要措辞
- [x] 产出 delta 文件到 `deltas/prd/3-technical-plan/2-scenario-implementation/core-S13-verify-results.md` — MODIFIED EX-3.2（非白名单写入语义收窄：依赖目录豁免，精确段匹配）+ ADDED EX-3.3（依赖准备写入豁免，infos 通道 + 白名单定点回收）+ ADDED EX-3.4（symlink 逃逸：启动前拓扑按无法隔离处理 + 运行期动态逃逸由写保护在写入前阻断 + 能力分层）；同步沙箱执行步骤描述
- [x] 产出 delta 文件到 `deltas/prd/3-technical-plan/2-scenario-implementation/core-S19-smoke-gate.md` — MODIFIED EX-4.3（精确段匹配收窄）+ ADDED EX-4.4（依赖准备写入豁免，语义与 S13 共享执行器一致，含 symlink / 定点回收引用）
- [x] 产出 delta 文件到 `deltas/test/core-S13-test-cases.md` — ADDED 新增用例：UT-S13-47（node_modules 写入豁免 + infos 通道断言）、UT-S13-48（精确段匹配负例：node_modules-cache / my-node_modules / node_modules.txt 仍 always FAIL / auto warn + 分隔符归一）、UT-S13-49（monorepo 嵌套豁免）、UT-S13-50（内部相对 symlink 保持相对语义、原 workspace 字节不变）、UT-S13-51（启动前逃逸 symlink 按无法隔离处理不静默豁免）、UT-S13-52（result_path 位于 node_modules 下定点采集回收）、UT-S13-53（运行期新建绝对逃逸链接写入被写保护阻断、原 workspace 零改动）、UT-S13-54（运行期 retarget 内部链接写入被写保护阻断）、UT-S13-55（写保护不可用时能力分层 always FAIL / auto warn）、ST-S13-14（模拟 pnpm 依赖修复式写入端到端 verify PASS）
- [x] 产出 delta 文件到 `deltas/test/core-S19-test-cases.md` — ADDED 新增用例：UT-S19-08（smoke 沙箱 node_modules 写入豁免 + infos 通道）、UT-S19-09（smoke result_path 位于 node_modules 下定点采集回收）

## [code] 代码实现
> 删后续自检：六维打分 9（影响范围 1 + 行为复杂度 2 + 契约变化 1 + 测试规模 2 + 风险 2 + 不确定性 1）≥ 8 达大任务；候选垂直切法「A 审计豁免+定点回收+infos / B symlink 隔离+运行期写保护」逐片证伪——本提案全部 11 个新增 UT/ST ID 已合入 `logos/resources/test/`，`openlogos verify` 永远全量计算覆盖度，删掉任一后续片都会因该片 ID 无结果而覆盖不足 FAIL（(a) 否）；且 A/B 共享 `runSandboxedCommand` 同一执行器的复制语义、快照与审计次序改造，互相咬合不可独立闭环。任何垂直切法均过不了删后续门，按逃生口显式单切（合规结果）。
- [x] 单切片：verify / smoke 沙箱写入审计依赖目录豁免与运行期写保护整体落地——`cli/src/lib/sandbox.ts`：复制保持 symlink 目标字面量（等价 `verbatimSymlinks`）+ 复制后 realpath containment 校验（逃逸链接按无法隔离处理）；OS 级运行期写保护（macOS `sandbox-exec` 拒写原 workspace 子树 / Linux mount namespace 只读绑定 / 能力探测），不可用时能力分层（`always` FAIL 说明原因、`auto` warn 披露残留风险）；依赖目录豁免按「规范化 + 分隔符归一后至少一个完整路径段严格等于 `node_modules`」精确段匹配（禁止子串/前缀/后缀），快照遍历跳过豁免目录；copy-back 改为白名单定点采集（存在即拷回、不依赖快照 diff、白名单优先于豁免）；`SandboxData` 新增可选 `infos: string[]` 通道并在豁免生效时追加一条固定信息级说明。`cli/src/commands/verify.ts` / `smoke.ts`：infos 不复制进 `pre_run.diagnostics` / 问题诊断，文本输出以 `ℹ️` 渲染一次且不用 `⚠️`，`sandbox.status` 不受 infos 影响。同步更新根目录 `spec/workflow.md`（工作区写入保护小节：依赖目录豁免 + symlink 隔离与运行期写保护 + 白名单定点回收）与 `spec/cli-json-output.md`（`sandbox.infos` 字段定义）及 `logos/spec/` dogfood 副本、官网文档 `website/src/content/docs/cli/verify.md`、`cli/smoke.md`、`zh/cli/verify.md`、`zh/cli/smoke.md`。补齐 `cli/test/s13-verify.test.ts` / `cli/test/s19-smoke.test.ts` UT/ST 测试代码 + OpenLogos reporter（写入 `logos/resources/verify/test-results.jsonl`），并更正既有 `s19-smoke.test.ts` 沙箱阻断测试与规格 `ST-S19-06` 的 ID 冲突（改指规格中语义相符的真实 ID，不新增规格条目）；保持既有沙箱用例与全量回归通过（覆盖 UT-S13-47、UT-S13-48、UT-S13-49、UT-S13-50、UT-S13-51、UT-S13-52、UT-S13-53、UT-S13-54、UT-S13-55、ST-S13-14、UT-S19-08、UT-S19-09）
