# 变更提案：OpenCode 用户使用指南文档

## 背景

OpenCode 已作为一等 AI 工具支持，且随 `init`/`sync` 部署本地插件与 `opencode.json`。README 中仅有简略说明，缺少独立、可维护的完整使用指南。

## 目标

新增 `docs/opencode.md`（中文），覆盖初始化、目录结构、插件命令、日常协作与常见问题；在 `README.md` 中增加显式链接，避免与 Claude Code 章节混淆。

## 影响

- 新增：`docs/opencode.md`
- 修改：`README.md`（OpenCode 小节增加「完整指南」链接并适度精简重复内容）
