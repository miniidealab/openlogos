# Delta: core-S11-test-cases.md

> change: lite-cut1b-remove-merge-transaction
> 目标：`logos/resources/test/core-S11-test-cases.md`

## REMOVED — S11 合并事务只读状态测试用例

`status` 携带 `merge_transaction` 只读投影的用例随事务删除。

## REMOVED — S11 完整公共事务只读投影测试用例

完整事务投影（相位、allowed_actions、content_slots）用例随事务删除。

## REMOVED — S11 Reopen 状态权威与残留字节测试

reopen 状态权威依附于事务终态与重开通道，随之删除。
