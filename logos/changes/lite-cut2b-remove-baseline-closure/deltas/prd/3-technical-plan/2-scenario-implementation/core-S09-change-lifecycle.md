# Delta: core-S09-change-lifecycle.md

> change: lite-cut2b-remove-baseline-closure
> 目标：`logos/resources/prd/3-technical-plan/2-scenario-implementation/core-S09-change-lifecycle.md`

## REMOVED — S39 按触达闭包接入 S09 change 生命周期

本节描述闭包计划如何接入 change 生命周期：write-proposal 阶段建立 touched 场景与 targets、write-tasks 阶段按 targets 投影 `[delta]` 任务、merge 阶段做 P==T==D 对账。L9 删除后 merge 目标集改由 `deltas/` 目录派生，本接入时序失去描述对象。

change 生命周期的其余环节（创建、产 delta、merge、verify、archive）逐条不变。
