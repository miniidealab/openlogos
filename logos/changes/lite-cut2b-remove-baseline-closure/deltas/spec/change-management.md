# Delta: change-management.md

> change: lite-cut2b-remove-baseline-closure
> 目标：`spec/change-management.md`

## REMOVED — 按触达目标规格闭包（S39 / on-touch-v1）

本节定义 `baseline_closure` 声明的 schema、touched 场景的四维强制、各维度的 SKIP/AMBIGUOUS 证据要求、targets 排序规范与 P==T==D 对账口径。L9 删除后 merge 目标集改由 `deltas/` 目录的无逻辑投影得到，本节失去定义对象。

变更流程的其余合同（创建、产 delta、merge、verify、部署、smoke、archive 的各自确认点）逐条不变。
