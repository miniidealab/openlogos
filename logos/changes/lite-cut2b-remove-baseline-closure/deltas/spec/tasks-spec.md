# Delta: tasks-spec.md

> change: lite-cut2b-remove-baseline-closure
> 目标：`spec/tasks-spec.md`

## REMOVED — on-touch-v1 的 delta 目标模式与唯一性

本节定义 `[delta]` 任务如何按闭包 targets 投影（每个非 SKIP 目标恰一条任务、标注 `[MODIFY]`/`[CREATE]`）及其与 proposal 权威闭包声明的一致性要求。L9 删除后 `[delta]` 任务不再承担与计划对账的职责，本节失去定义对象。

`[delta]` 段的基本结构要求（可解析、路径合法、勾选语义）由本规范其余章节继续约束。
